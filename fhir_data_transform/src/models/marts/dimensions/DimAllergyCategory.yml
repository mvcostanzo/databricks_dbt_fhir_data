version: 2
snapshots: 
  - name: DimAllergyCategory
    relation: ref('snap_dim__allergycategory')
    config: 
      database: marts 
      schema: allergy 
      alias: DimAllergyCategory
      unique_key: AllergyCategoryID
      dbt_valid_to_current: "to_date('9999-12-13', 'yyyy-MM-dd')"
      snapshot_meta_column_names:
        dbt_valid_from: valid_from
        dbt_valid_to: valid_to
      strategy: check
      check_cols: all